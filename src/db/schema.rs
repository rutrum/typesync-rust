table! {
    scores (name, genius_id, milliseconds, absolute_time, mode) {
        name -> Varchar,
        genius_id -> Varchar,
        milliseconds -> Bigint,
        absolute_time -> Bigint,
        mode -> Tinyint,
    }
}
